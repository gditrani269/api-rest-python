from setuptools import setup, find_packages

setup (
    name = 'OpenShift Api client',
    version='1.0.0',
    description='Uses official Python client library for kubernetes',
    url='https://github.com/gditrani---/api-rest-python',
    author='xx',
    author_email='a@a.com.ar',
    keywords='openshift api client',
    include_package_data=True,
    packages=find_packages()
)